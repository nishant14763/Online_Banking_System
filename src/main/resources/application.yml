server:
  port: 8081
spring:
  application:
    name: banking-app
  datasource:
    url: jdbc:mariadb://localhost:3306/banking_app
    username: root
    password: Password@1
  jpa:
   hibernate:
    ddl-auto: update
   show-sql: true